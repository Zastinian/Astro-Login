---
let cookie = "none";
let redir = "/login";
if (Astro.request.method === "POST") {
  const cook: string = `${Astro.cookies.get("login")?.value}`;
  if (cook) {
    cookie = cook;
  }
} else {
  return Astro.redirect("/");
}
---

<script
  define:vars={{
    cookie,
    redir,
  }}
  is:inline
>
  if (cookie !== "none") {
    document.cookie = `login=${cookie}; Max-Age=0; path=/`;
    window.location.href = redir;
  }
</script>
